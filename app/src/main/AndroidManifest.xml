<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.suoemi.ece8803proj">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label=""
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SignupActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity"></activity>
        <activity android:name=".BuyerMain"></activity>
        <activity android:name=".SellerMain"></activity>
        <activity android:name=".SellerProfile"></activity>
        <activity android:name=".SellerInput"></activity>
        <activity android:name=".PaymentInput"></activity>
    </application>

</manifest>